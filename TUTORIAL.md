# 業務担当者のための階層クラスタ分析・実践ガイド

本ドキュメントは、統計学の専門知識がなくても「簡易クラスタ分析ツール」を活用し、実務でデータセグメンテーションや顧客分類を行うための実践ガイドです。

---

## 1. クラスタ分析とは？

クラスタ分析（Cluster Analysis）とは、多次元のデータの中から**「似た性質を持つデータ同士を集めてグループ（クラスタ）に分類する手法」**です。

### 活用例
- **顧客セグメンテーション**: 購買金額・頻度・Web行動データから「優良顧客」「コスパ重視層」「休眠予備軍」などに分類。
- **店舗・エリア分類**: 売上・客層・立地データから、似た傾向を持つ店舗グループを抽出して施策を最適化。
- **商品分類**: 評価や属性データから嗜好パターンごとの製品グループを特定。

---

## 2. 階層クラスタ分析とウォード法

本ツールで採用している**階層クラスタリング（Hierarchical Clustering）**は、個々のサンプルからスタートし、最も近いもの同士を順に結合していく手法です。

- **ウォード法 (Ward's Method)**:
  クラスタ内部のデータのバラツキ（分散）の増加量を最小にするように結合を進める方法です。最もサイズが均等で明確なグループが作られやすいため、実務において最も推奨されます。

---

## 3. 分析手順とポイント

1. **データ準備**:
   - 1行目: 変数名（例: 売上高, 来店回数, 顧客満足度）
   - 1列目: サンプル識別名（例: 顧客ID, 店舗名, 都市名）
   - 2列目以降: 全て数値データ

2. **前処理（標準化 Z-Score）**:
   - 「金額 (数万円)」と「回数 (数回)」のように、変数の単位や桁数が異なる場合は必ず「標準化 (Z-Score)」を有効にしてください。単位の違いによる歪みを防ぎます。

3. **クラスタ数の選択**:
   - ツリー構造の樹形図（デンドログラム）で、縦の結合高さが急激に高くなる直前で切断するのが基本です。
   - ツリー上の「自動推奨クラスタ数」ボタンを活用すると、統計的なシルエット係数に基づいた最適な分割が自動設定されます。

4. **エクセル出力と施策への活用**:
   - ダウンロードしたエクセルファイルの `全データと割当` シートには各サンプルがどのクラスタに所属するかが記録されています。
   - `クラスタ要約統計` シートの各変数の平均値を比較し、それぞれのクラスタの特徴に合わせたネーミング（例：「高単価・高頻度グループ」など）をつけて実務に役立ててください。
